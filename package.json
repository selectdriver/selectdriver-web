{
  "name": "selectdriver-web",
  "version": "1.0.0",
  "description": "Corporate website for SelectDriver with serverless email backend",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "nodemailer": "^6.9.8"
  },
  "engines": {
    "node": ">=14"
  }
}
